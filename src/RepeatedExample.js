import styled from 'styled-components';

const RepeatedExample = styled.div`
    padding: ${props => props.small ? '5px' : '10px'};
    width: 100%;
    padding: 10px;
`;

export default RepeatedExample;